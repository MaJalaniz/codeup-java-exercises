import java.io.*;
import java.util.*;

public class ArraysExercises {
    /*
    public static Person[] addPerson(Person[] people, Person[] people){
        Person[] morePeople = Arrays.copyOf(people, people.length);
        morePeople[morePeople.length]
        return morePeople;
    }
    //people=addPerson(people, new Person());

    public static void main(String[] args) {
        Person[] people = {new Person("Zodiark", "Avis"), new Person("Omega", "Mark-XII"), new Person("Adrem", "Sandsea")};
        for (Person person : people) {
            System.out.println(person.getFullName());// use full name to cut down on code above ^^

            //for (int i = 0; i < people.length; i++)
        }
    }
*/





}